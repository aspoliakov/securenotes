package com.aspoliakov.securenotes.core_presentation.navigation

/**
 * Project SecureNotes
 */

enum class AppGlobalNavigation(val route: String) {
    Splash(route = "splash"),
    Auth(route = "auth"),
    Main(route = "main"),
}
