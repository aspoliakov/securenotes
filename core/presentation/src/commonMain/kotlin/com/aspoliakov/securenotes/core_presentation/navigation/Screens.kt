package com.aspoliakov.securenotes.core_presentation.navigation

/**
 * Project SecureNotes
 */

enum class Screen(val route: String) {
    Splash(route = "splash"),
    Auth(route = "auth"),
    Home(route = "home"),
}
